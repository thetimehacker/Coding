#include <bits/stdc++.h>
using namespace std;

void sol(){
	int t=1;
	cin>>t;
	while(t--){
		string s;
		cin>>s;
		stack<char> st;
		int mx=0;
		int count=0;
		for(auto x:s){
			if(x=='<')st.push(x);
			else
		}
	}
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    sol();

}