#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void call(){
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
}

int main() {
    //call();
    //Start from here
    int t;
    cin>>t;
    while(t--){
    	ll a,b;
    	cin>>a>>b;
    	ll maxx=max(a,b);
    	ll minn=min(a,b);
    	ll ntwo=max%2;
    	
	}
}
